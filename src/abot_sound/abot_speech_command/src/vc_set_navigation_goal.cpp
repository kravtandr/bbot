#include <ros/ros.h>
#include "vc_set_navigation_goal.h"
#include "functions.cpp"

int main(int argc, char **argv) {
/*
	ros::init(argc, argv, "vc_set_navigation_goal");
	ros::NodeHandle nh;
 	std::string coords;
 	nh.getParam("coords", coords);
	std::ifstream fin(coords);
	fin.close();
	VCSetNavigationGoal set_navigation_goal;
	ROS_INFO("Voice command node 'Set Navigation Goal': Start.");
	std::string line;
	std::getline(fin,line); // headers
	ROS_INFO("headers: %s", line.c_str());
	ROS_INFO("file: %s", coords.c_str());
	ros::spin();
*/
	return 0;
}
